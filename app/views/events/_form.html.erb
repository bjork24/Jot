<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <label>Month / Day</label><br />
    <input maxlength="2" type="text" name="event_date[month]" />
    &nbsp;&nbsp;/&nbsp;&nbsp;
    <input maxlength="2" type="text" name="event_date[day]" />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
